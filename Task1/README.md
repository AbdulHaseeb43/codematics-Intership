
A professional web interface built to demonstrate mastery of semantic HTML5 and CSS Flexbox positioning.

## 🚀 Features Implemented
- **Full Viewport Layout:** The design fits exactly 100% of the browser height ($100vh$) with no scrolling.
- **Semantic Structure:** Uses header,  section, aside, main, and footer for accessibility.
- **Flexbox Engine:** Implements a vertical stack for the page and a horizontal row for the content area.
- **Dynamic Resizing:** The main section uses flex: 1 to fill all available vertical space automatically.
- **Clean Aesthetic:** High-contrast black text on a light monochrome background.

## 🛠️ Technologies Used
- **HTML5:** For structured, semantic markup.
- **CSS3:** For layout (Flexbox), box-model styling, and viewport units.

## ⚙️ Project Setup Steps
1. **Clone the repository:**
   `git clone https://github.com/AbdulHaseeb43/-Web-Framework.git`
2. **Open the project:**
   Navigate to the folder and open task1.html in any modern web browser (Chrome, Firefox, or Edge).
3. **No Installation Required:** This is a pure frontend project and does not require a local server or dependencies.
